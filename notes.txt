cd ~/projects/codex-workflow-kit && bash scripts/install.sh ../moggoff --force

cd ~/projects/codex-workflow-kit && bash scripts/install.sh ../karebraids/dev --force


//todo
Audit and complete the worktree-safe workflow model in codex-workflow-kit.

Context:
This repo already moved most workflow artifacts to run-scoped paths like _workflow/runs/<run-id>/.
The goal is to fully eliminate workflow-state merge conflicts when using long-lived git worktrees (main, dev, redesign, feature branches, etc.) where separate Codex agents work independently.

Problem:
The current model is close, but still incomplete:
- WORK_REQUEST.md is still shared and auto-managed, so separate worktrees can overwrite each other.
- RUN_WORKFLOW.md references parallel templates that may not exist.
- Installer may not ship all required templates.
- Need final consistency across README, RUN_WORKFLOW, templates, and installer.

Goal:
Make workflow state fully worktree-safe, run-scoped, and merge-safe.

Required work:

==================================================
1. Make request state run-scoped
==================================================

Current issue:
WORK_REQUEST.md is still shared.

Change model:
Move active request state into run scope.

New path:
_workflow/runs/<run-id>/request.md

Rules:
- Active workflow request state must live in:
  _workflow/runs/<run-id>/request.md
- Root WORK_REQUEST.md becomes optional/manual compatibility only.
- Agents must not auto-update root WORK_REQUEST.md during normal worktree runs.
- Parallel workers/orchestrators must use run-scoped request state.

Update all workflow docs and instructions to use:
<artifact-root>/request.md

instead of shared WORK_REQUEST.md where appropriate.

==================================================
2. Create missing parallel templates
==================================================

Add these committed templates:

templates/_workflow/runs/parallel/claims.md
templates/_workflow/runs/parallel/locks.md
templates/_workflow/runs/parallel/agent-status.md

Template expectations:

claims.md
Must track:
- task id
- worker id
- branch
- worktree path
- run id
- claim status
- start time
- end time
- files expected
- files changed
- verification status

locks.md
Must track:
- file path
- lock owner
- worker id
- task id
- run id
- reason
- lock status
- acquired at
- released at

agent-status.md
Must track:
- worker id
- role
- branch
- worktree path
- run id
- current task
- changed files
- verification result
- blocker
- final status

==================================================
3. Update installer
==================================================

Update scripts/install.sh.

Must install:
- _workflow/index.md
- _workflow/runs/README.md
- _workflow/runs/parallel/claims.md
- _workflow/runs/parallel/locks.md
- _workflow/runs/parallel/agent-status.md

Also update setup messaging:
Root WORK_REQUEST.md is compatibility/manual only.
Primary active workflow state is run-scoped.

==================================================
4. Update RUN_WORKFLOW.md
==================================================

Refactor to fully use run-scoped request state.

Replace shared request assumptions.

Rules:
- Detect:
  current branch
  worktree path
  run id
  artifact root
- Request file:
  <artifact-root>/request.md
- Handoff:
  <artifact-root>/handoff.md
- Progress:
  <artifact-root>/progress.md
- Spec:
  <artifact-root>/spec.md
- Tasks:
  <artifact-root>/tasks.md
- Review:
  <artifact-root>/review.md
- Verification:
  <artifact-root>/verification.md
- Release notes:
  <artifact-root>/release-notes.md
- Summary:
  <artifact-root>/summary.md

Parallel safety:
Workers must never update another run folder.
Workers must never update shared workflow state except append-only files.

Shared files allowed:
- _workflow/index.md (append-only)
- _workflow/runs/README.md (static or append-only)

==================================================
5. Update README.md
==================================================

README must match final behavior.

Document:
Run-scoped request state:
_workflow/runs/<run-id>/request.md

Example:

repo.git/
main/
  _workflow/runs/main/
dev/
  _workflow/runs/dev/
redesign/
  _workflow/runs/redesign/

Each run folder contains:
request.md
spec.md
tasks.md
progress.md
handoff.md
review.md
verification.md
release-notes.md
summary.md
parallel/

Explain:
No active workflow artifact should require multiple branches to edit the same file.

==================================================
6. Keep compatibility
==================================================

Do NOT break older repos.

Legacy support:
- root WORK_REQUEST.md may still exist
- old _spec/_task/etc may still exist

But:
All new active workflow runs default to:
_workflow/runs/<run-id>/

==================================================
7. Add merge safety rules
==================================================

Explicit rules:
- Never merge workflow reports line-by-line manually.
- Preserve each run folder.
- Regenerate aggregate/index state after merge.
- Treat run folders as branch/worktree-local workflow memory.
- Final orchestration happens after branch merge.

==================================================
8. Final audit
==================================================

After implementation, audit and report:

1. Are ALL active workflow artifacts run-scoped?
2. Is root WORK_REQUEST now compatibility-only?
3. Do parallel templates exist?
4. Does installer ship them?
5. Are README and RUN_WORKFLOW consistent?
6. Can dev/redesign/main worktrees run without workflow-state merge conflicts?
7. Remaining gaps.

Output verdict:
PASSED / PARTIAL / FAILED
with exact missing items if any.

Do a full consistency pass across:
README.md
templates/RUN_WORKFLOW.md
scripts/install.sh
templates/_workflow/*